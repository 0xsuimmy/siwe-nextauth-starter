import type { NextPage } from "next";
import styles from "../styles/Home.module.css";

const Home: NextPage = () => {
  return <div style={{ textAlign: "center" }}>Sign in with Ethereum Starter</div>;
};

export default Home;
